package Items;

public class ValveOil extends Items {
    public ValveOil(ItemTypes itemTypes, String name, double buyingPrice, double sellingPrice) {
        super(itemTypes, name, buyingPrice, sellingPrice);
    }
}
