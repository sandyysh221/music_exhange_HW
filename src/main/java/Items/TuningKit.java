package Items;

public class TuningKit extends Items {
    public TuningKit(ItemTypes itemTypes, String name, double buyingPrice, double sellingPrice) {
        super(itemTypes, name, buyingPrice, sellingPrice);
    }
}
