package Items;

public class ShoulderRest extends Items {
    public ShoulderRest(ItemTypes itemTypes, String name, double buyingPrice, double sellingPrice) {
        super(itemTypes, name, buyingPrice, sellingPrice);
    }
}
