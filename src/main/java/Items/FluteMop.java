package Items;

public class FluteMop extends Items {

    public FluteMop(ItemTypes itemTypes, String name, double buyingPrice, double sellingPrice) {
        super(itemTypes, name, buyingPrice, sellingPrice);
    }
}
