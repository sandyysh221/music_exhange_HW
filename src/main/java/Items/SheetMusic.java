package Items;

public class SheetMusic extends Items {
    public SheetMusic(ItemTypes itemTypes, String name, double buyingPrice, double sellingPrice) {
        super(itemTypes, name, buyingPrice, sellingPrice);
    }
}
